================================================================================
ANSWERS — FRESHCART BACKEND DESIGN REVIEW
================================================================================
Excellent review. Nearly all of it is accepted. Where I say ACCEPT, take your
own recommendation as written and put it in the document.

Read Q5 carefully — your diagnosis is right but your inferred rule is also
wrong, and the fixture is at fault.


================================================================================
A. BLOCKING QUESTIONS
================================================================================

Q1  CMS-SCOPE.txt — DECLARE IT DEAD.
    It existed and has been deleted. Do not ask for it. Fold module scope and
    build order into BACKEND-DESIGN.txt as new sections, so there is exactly
    one normative document. Remove all four citations.

Q2  The Admin audit is CONFIRMED — I verified it independently.
    No fetch, no axios anywhere in Admin/src. Order ids are '10486'. Type is
    'Curbside'. Everything renders from Admin/src/lib/data.js constants.
    "90% built" meant visually complete, not integration-ready. Your reading
    is correct.
    - Yes, this is the real admin. There is no other build.
    - Yes, the backend defines the admin's client contract.
    - Treat Admin/src/lib/data.js as a REQUIREMENTS document, exactly as you
      proposed. Mine it for: barcode-driven QuickStock updates, rider
      assignment + availability, credit overdue flags and last-payment dates,
      and the full Insights metric set.
    - Plan for a full data-layer build across ~11 screens. Say so in the
      build order section.

Q3  ACCEPT. Nested envelope wins; the app client changes.
    Confirmed: App/src/api/client.ts reads (body.message) and (body.code) at
    the top level. It is a three-line change and I will make it on the app
    side. Add requestId to the envelope while you are there.

Q4  ACCEPT your recommendation, and log it as a DEPARTURE.
    Confirmed the app uses human slugs: 'fruits-veg', 'fresh-fruit', 'p1',
    'mineral-water-default'.
    - ObjectId as internal _id.
    - slug is the public identifier for categories and subcategories.
    - products and variants expose ObjectId strings.
    - One-time cart loss at cutover is ACCEPTED. CartContext already drops
      unresolvable lines without crashing, so no migration is needed.
    You are right that this rename was undocumented. Fix the section 3 claim.

Q5  YOUR RULE IS ALSO WRONG. The fixture is at fault, not the formula.
    Check the numbers:
      unsettled only   = 39 + 84.75            = 123.75
      all entries      = 39 - 200 + 84.75 + 79.75 = 3.50
      fixture balance  =                          203.50
    203.5 is the sum of the three PURCHASES with the payment ignored
    entirely. Neither of the candidate rules reproduces it.

    This is bad mock data, not a hidden business rule. Do not reverse-engineer
    from it.

    THE RULE IS:
      balance = sum(all entries)   purchases positive, payments negative
      `settled` is a DISPLAY flag for the ledger UI. It is never an input to
      the balance.

    ACTION: fix the fixture during seeding — balance becomes 3.50 — and write
    the section 9 assertion against sum(all). If 3.50 looks too low to demo
    credit sensibly, change the seeded payment to -50 and set balance to
    153.50. Either is fine; an incoherent fixture is not.

    SETTLEMENT: ACCEPT your recommendation. FIFO oldest-first, no entry
    splitting, plus an explicit unappliedCredit field on the account.

Q6  ACCEPT, and this is the best catch in the review.
    Oversell is accepted for v1 — substitution is normal in grocery. But add
    the fulfilment path the design lacks:
      - line-level state: fulfilled | substituted | unavailable
      - order-level outcome: partially_fulfilled
      - adjusted total, with a refund or a compensating credit entry
    Do NOT build quantity tracking. It contradicts the app's stated model.

Q7  ACCEPT. Single-node replica set in dev via Docker, Atlas in prod.
    Put it in section 1 as a hard requirement, and note that
    mongodb-memory-server needs replica-set mode for the transaction tests.

Q8  ACCEPT exactly as written. UTC instants, APP_TZ=Asia/Dubai for every date
    boundary, plain YYYY-MM-DD for dueDate interpreted in APP_TZ.

Q9  ACCEPT your matrix, with two additions:
      staff    order queue, stock flags, arrival queue
      manager  + catalogue, promos, credit approval, banners
      owner    + admin users, settings, refunds, credit limit changes
    Refunds are owner-only. Audit log is visible to owner and manager.

Q10 BUSINESS DECISION — I cannot answer this one; it is not in the code.
    Flag it as an open item with an owner and a date. Your point stands: it
    is a one-word schema field with a multi-week lead time, and it should be
    started now. For UAE, assume an aggregator with a registered sender ID.
    Design the OtpChannel abstraction so the provider is swappable, and ship
    a console-logging provider for development.

--------------------------------------------------------------------------------
IMPORTANT (not day one)
--------------------------------------------------------------------------------

Q11 ACCEPT. Confirmed the type comment says "absent for milestones that have
    not happened yet". Store append-only server truth; the app derives future
    milestones from flowFor(fulfillment), which already exists client-side.
    I will handle the app side.

Q12 ACCEPT — CUT social login entirely.
    Confirmed: the buttons were removed from LoginScreen earlier;
    signInWithProvider in api/auth.ts and AuthContext is dead code. I will
    delete it app-side. Your App Store 4.8 point is correct and decisive.

Q13 ACCEPT, including your disagreement. 60-minute access token, rotating
    refresh with a refreshTokens collection, replacedBy chain, family
    revocation on reuse. Refresh in Expo SecureStore, access in memory.

Q14 ACCEPT. Cart stays stateless and client-owned. No cross-device cart in
    v1. Note the abandoned-cart gap in the "later" section so it is a known
    trade-off rather than a surprise.

Q15 ACCEPT. Customer may cancel only while `placed`. Cancellation writes a
    compensating ledger entry in the same transaction as the status change.
    Never mutate the original entry.

Q16 ACCEPT. Add availableFor: [delivery|pickup] to paymentMethods.

Q17 ACCEPT. Nightly dailyRollups keyed by { date, metric }, live query for
    the current day only.

Q18 ACCEPT. Move upload out of "later" into the first build. Presigned PUT
    direct to R2, 5MB cap, server-side content-type validation, never proxy
    bytes through Express.

Q19 ACCEPT — this is the right resolution.
    status: draft | published on catalogue documents. Drafts may hold a
    partial Localized; published requires both languages. Public endpoints
    return published only, so the app's no-fallback assumption still holds.

Q20 ACCEPT. Your MongoDB fact is correct — Arabic stemming is not in
    Community text indexes. Use a normalized, diacritic-stripped
    searchTokens array with a normal index. Atlas Search later.

Q21 ACCEPT. Guarded subcategory delete (block on non-zero product count or
    reassign) plus an integrity report endpoint for the CMS.

Q22 ACCEPT. Idempotency-Key header on POST /orders, 24h TTL, repeat returns
    the original order.

Q23 ACCEPT. Atomic findOneAndUpdate with redemptions < maxRedemptions in the
    filter, perCustomerCap counted in the same transaction.

Q24 ACCEPT. FC-#### wins. The Admin adapts.

--------------------------------------------------------------------------------
SMALLER
--------------------------------------------------------------------------------

Q25 YES — /api/v1 from the start. I will update the app's base URL.
Q26 DROP currency as configurable. Keep it a constant. See your disagreement
    7, which I agree with.
Q27 ADD visible: boolean. Hidden and archived are different states, and the
    Admin already distinguishes them.
Q28 YES — categoryId may be null. Public endpoints exclude uncategorised
    products; the CMS surfaces them in the Uncategorised bucket for triage.
Q29 v1: riders do NOT log in. Staff update rider state on their behalf.
    But add the fields the Admin already tracks to schema 3.12: vehicle,
    availability (on_delivery | available | off_shift), assignedOrderIds.
Q30 Changed-fields diff, not full snapshots. You are right that product
    snapshots with embedded variants get large fast.
Q31 ACCEPT. Cursor pagination for orders and the credit ledger. Keep offset
    with total where a human needs page numbers in the CMS.
Q32 YES to both, no objection.


================================================================================
B. THE THREE OPEN DECISIONS
================================================================================

1. DELIVERY SLOTS — DROP. Agreed, with one flag.
   Your verification matches mine: slotDays/slotsForDate are referenced
   nowhere, SlotPickerSheet is imported by no screen, slotLabel is never
   read. It is dead code and I will delete it app-side.

   THE FLAG: the Admin's own mock data assumes slots exist —
   Admin/src/lib/data.js line 6 has a delivery order detailed as
   "Apt 1204, Marina Heights · slot 1–3 PM". So the CMS was designed with
   scheduling in mind even though the app dropped it.

   That is a design-intent signal, not a dependency. Proceed with DROP, but
   the Admin must drop it too, and someone should confirm with the business
   that scheduled delivery is not returning within ~6 months. If it is,
   say so now — your point about it changing capacity planning, picking and
   the state machine is exactly right.

2. SINGLE BRANCH — Agreed, including your seam.
   Build single-branch. Keep pickupStores as a real collection, not a
   settings field. Put storeId on every order from day one, always
   populated. Then multi-branch is "add a stock collection", not "add a
   store concept".
   I have flagged the second-branch question to the business.

3. ENDPOINT SHAPES — Backend defines. Premise correction accepted.
   Confirmed: the Admin calls nothing. There is no contract to adapt to.


================================================================================
C. YOUR DISAGREEMENTS
================================================================================
Agreed with 1, 2, 3, 4, 5, 6, 7 and 9 as written. Implement them.

Point 5 in particular is right and important: if a line goes out of stock
between pricing and submit, POST /orders must 409 with the re-priced basket
and the app re-confirms. Silently dropping a line charges the customer for a
basket they did not agree to. Same class as the stale-total case.

Point 8 — YOU ARE WRONG, see Q5. The fixture does not balance under
sum(unsettled) either. Write the assertion against sum(all entries) and fix
the fixture. Do not encode the fixture's arithmetic as a rule.


================================================================================
D. ADDITIONS
================================================================================
All 18 approved. Add them all.

Priority order — the route table is the single biggest omission and blocks
everyone. Do these three first:

  1. The route table (every public and /admin route, with method, auth,
     request/response shape, error codes)
  2. Auth specification (OTP lifecycle, refresh rotation, admin login)
  3. Concurrency and transaction boundaries table

Then the rest in your listed order.

ONE ADDITION TO YOUR LIST: keep the client migration plan (your item 18) but
scope it as APP-SIDE ONLY, and list the Admin's changes separately. The two
clients have entirely different migration profiles — the app is adapting a
working integration, the Admin is building one from nothing.

The app-side list I already own, from your notes:
  - error envelope (3 lines in api/client.ts)
  - Category.image -> imageUrl, with CategoryImage falling back to the icon
  - fils conversion at the utils/format.ts boundary only
  - id/slug changes and one-time cart invalidation
  - refresh token handling in SecureStore
  - delete the slot module and signInWithProvider
  - events: derive future milestones from flowFor


================================================================================
NEXT
================================================================================
Produce the updated design document in full, with the new sections. No code
yet. Where a business decision is still open (Q10 OTP provider, second
branch, slots confirmation), mark it clearly in the document rather than
assuming an answer.
